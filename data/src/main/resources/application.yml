server:
  port: 8083

spring:
  datasource:
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin}
    url: ${DB_URL:jdbc:postgresql://localhost:${DB_PORT:5433}/orders_db}
  jpa:
    hibernate:
      ddl-auto: update